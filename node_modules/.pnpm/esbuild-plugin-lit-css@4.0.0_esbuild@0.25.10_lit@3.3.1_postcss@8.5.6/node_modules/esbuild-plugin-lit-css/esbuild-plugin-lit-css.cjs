var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// packages/esbuild-plugin-lit-css/esbuild-plugin-lit-css.ts
var esbuild_plugin_lit_css_exports = {};
__export(esbuild_plugin_lit_css_exports, {
  litCssPlugin: () => litCssPlugin
});
module.exports = __toCommonJS(esbuild_plugin_lit_css_exports);
var import_lit_css = require("@pwrs/lit-css");
var import_promises = require("node:fs/promises");
function litCssPlugin(options) {
  const { filter = /\.css$/, specifier, tag, ...rest } = options != null ? options : {};
  return {
    name: "lit-css",
    setup(build) {
      const loader = "js";
      build.onLoad({ filter }, async (args) => {
        const css = await (0, import_promises.readFile)(args.path, "utf8");
        const filePath = args.path;
        try {
          const contents = await (0, import_lit_css.transform)({ css, specifier, tag, filePath, ...rest });
          return { contents, loader };
        } catch (error) {
          return {
            errors: [{
              text: error.message,
              detail: error,
              location: {
                file: error.file,
                line: error.line,
                column: error.column
              }
            }]
          };
        }
      });
    }
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  litCssPlugin
});
//# sourceMappingURL=esbuild-plugin-lit-css.cjs.map
