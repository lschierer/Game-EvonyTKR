import { RowData, Table, TableOptions } from '@tanstack/table-core';
import { ReactiveController, ReactiveControllerHost, TemplateResult } from 'lit';
export * from '@tanstack/table-core';
export declare function flexRender<TProps>(Comp: ((props: TProps) => string) | string | TemplateResult | undefined, props: TProps): TemplateResult | string | null;
export declare class TableController<TData extends RowData> implements ReactiveController {
    host: ReactiveControllerHost;
    private tableInstance;
    private _tableState;
    constructor(host: ReactiveControllerHost);
    table(options: TableOptions<TData>): Table<TData>;
    hostDisconnected(): void;
}
