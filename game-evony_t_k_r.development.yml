---
# Development mode configuration
# Uses SQLite for all persistence (including non-Minion data)
aws:
  region: us-east-2  # Not actually used in dev, but defined for consistency
persistence:
  backend: sqlite
  sqlite_db_path: ./var/evonytkr.db
